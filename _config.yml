# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# Site settings
title: Memorial Service for Drew's Youth
description: >- # this means to ignore newlines until "baseurl:"
  A simple website to host a temporary birthday party agenda and related info.
baseurl: "/DrewGonnaDie" # the subpath of your site, e.g. /blog
url: "https://drewnielson.github.io/" # the base hostname & protocol for your site, e.g. http://example.com


# Build settings
plugins:
  - jekyll-feed

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - README.md
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - package-lock.json
  - pakage.json
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/
  - gulpfile.js
  - LICENSE